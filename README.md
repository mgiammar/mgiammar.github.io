# mgiammar.github.io

Welcome to the codebase serving my personal website.
This is a basic Jekyll site hosted on GitHub Pages.

## Setup and Serving

To set up the Jekyll environment, ensure you have Ruby and Bundler installed. Then, run:

```bash
bundle install
```

To serve the website locally, navigate to the `docs` directory and run:

```bash
bundle exec jekyll serve
```